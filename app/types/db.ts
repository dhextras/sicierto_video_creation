export type Video = {
  id: string;
  thumbnail: string;
  title: string;
};

export type ScriptPrompt = {
  id: string;
  title: string;
  prompt: string;
};

export type Script = {
  id: string;
  script: string;
};
